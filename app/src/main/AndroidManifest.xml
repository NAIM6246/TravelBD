<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.travelbd"
    tools:ignore="GoogleAppIndexingWarning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity android:name=".UserProfile"></activity>
        <activity android:name=".Bandarban" />
        <activity android:name=".Scroll_view" />
        <activity android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Auth_Home"
            android:label="@string/app_name"/>
        <activity android:name=".Home"
            android:label="@string/app_name"/>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".Divisions"
            android:label="@string/title_activity_divisions" />
        <activity android:name=".AddPlace" />
        <activity android:name=".Registration" />
        <activity
            android:name=".Login"
            android:label="Login"
            android:noHistory="true" />
        <activity android:name=".Show_Place" />
    </application>

</manifest>